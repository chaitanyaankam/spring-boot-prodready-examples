server:
  port: 8762
spring:
  application:
    name: books-zuul-gateway
zuul:
  prefix: /book-api    
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/