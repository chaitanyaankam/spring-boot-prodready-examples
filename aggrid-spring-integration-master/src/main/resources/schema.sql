CREATE TABLE IF NOT EXISTS olympics (
    athlete VARCHAR(255),
    country VARCHAR(255),
    year VARCHAR(255),
    sport VARCHAR(255),
    gold VARCHAR(255),
    silver VARCHAR(255),
    bronze VARCHAR(255)
);